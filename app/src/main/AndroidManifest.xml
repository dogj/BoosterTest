<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.boostertest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity" />
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fundTypes.Defensive" />
        <activity android:name=".fundTypes.Conservative" />
        <activity android:name=".fundTypes.Balanced" />
        <activity android:name=".fundTypes.BalancedGrowth" />
        <activity android:name=".fundTypes.Growth" />
        <activity android:name=".fundTypes.AggressiveGrowth" />
        <activity android:name=".Introduction" />
    </application>

</manifest>